# Dependabot configuration for automated dependency updates
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates

version: 2
updates:
  # Monitor npm dependencies
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    # Group all devDependencies together, and any other patch updates
    groups:
      dev-dependencies:
        dependency-type: "development"
      patch-updates:
        patterns:
          - "*"
        update-types:
          - "patch"
    # Auto-merge minor and patch updates (optional - can remove if you want manual review)
    labels:
      - "dependencies"
      - "automated"
    # Semantic versioning prefix
    commit-message:
      prefix: "chore"
      include: "scope"

  # Monitor GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      patch-updates:
        patterns:
          - "*"
        update-types:
          - "patch"
    labels:
      - "dependencies"
      - "automated"
    commit-message:
      prefix: "chore"
      include: "scope"
